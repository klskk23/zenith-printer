# 变更记录

按语义化版本记录**面向使用者的**行为变化。字段与接口的变化以 `docs/` 与 `specs/` 为准。

## 未发布（MINOR）

### 标签的四步：标签 · 设计 · 打印 · 确认

- 顶部常驻一条步条，标出你在四步中的哪一步。它**只标位置，不拦人**：画廊上点缩略图去
  改版面，点「打印」直接去打——日常从台账打标签不必先开编辑器。不设返回箭头：第 ① 步
  「标签」本身就是出口。
- **打印确认对话框拆成两页**：「打印」选机器、选参数、勾行、定每行份数，表格占满整页
  自己滚；「确认」列出打什么、打到哪、共几张，按下即出纸。提交后同一页变成回执：
  已加入队列几张、任务编号，以及去队列 / 再打一次 / 回到标签三个出口。
- **「确认」页把要打的那张画出来**：服务端按真实打印管线渲染（二值化后的位图，就是打印头
  要烧的那个）。绑了数据源时可以展开逐张看每一个选中的行，每页十张——条码的宽度跟着内容
  走，第 87 行可能超界而第 1 行完好，这是上卷纸之前唯一能发现的地方，被裁的那几张会自己
  标出来。没绑数据源只有一种样子，不给展开。
- **正常路径上一句提示也没有**：「打印会消耗标签纸」的常驻警示与打印头幅宽示意图都去掉了。
  只有真会出事时——有内容会被裁掉、标签比打印头宽、打印机没探测过——「确认」页才多一行，
  且**不阻止打印**。
- 「打印」页铺满整个宽度，行表不折行（长值横向滚动），一屏能看到更多行。
- 编辑器顶栏因此空出来：没有打印机与打印参数两个下拉了，撤销/重做搬到画布头部，底部是
  保存、另存为、继续。改了没保存也能直接去打印——打的就是眼前这张；离开整条流程才问
  「保留这些修改吗？」。
- 画廊不再写「标签」这个标题（步条已经说了），那一行让给队列状态。
- **旧地址继续可用**：`/design/{id}?preset=…` 现在落在打印页（`/labels/{id}/print?preset=…`），
  预设仍然只是初始值、仍然不会自动出纸。不带预设的 `/design/{id}` 照旧落编辑器。
  台账用 `POST /api/print-presets/{id}/print` 直接打印的那条路不经界面，完全不受影响。

### 界面上的话少了

- 常驻说明文字分三档：讲原理的删掉（「名称只是给人看的……」这类），讲怎么填的收进标题或
  字段旁的「?」，错误、不可撤销操作的确认、异常状态说明一字不动。
- Google 表格要分享给哪个地址，从数据源页正文移进标题旁的「?」：点开、可选中、带「复制」。

## 0.5.0 — 2026-09-21

### 界面重做：Nocturne

- 整个界面换成 Nocturne 设计系统：深色底、蓝紫强调只做线、圆角 4/8/14、描边按钮。
  只有一套配色，设置里不再有主题选项。
- **白只给纸。** 画布、缩略图、打印预览是界面里唯一的纯白，四周带微光——站在深色底上的
  不会被打印，只有白的才会。
- 界面文字的西文与数字改用 Inter（随包，约 40 KB）；中文不变。

### 导航：没有标签页栏，「标签」画廊就是家

- 侧栏是一整块：产品名在顶上，七项入口带图标（标签、数据源、打印机、打印队列、打印历史、
  打印预设、设置），打印机状态与服务连接状态在底部，从任何页面都看得到。
  没有首页、模板库与标签设计入口；接口调试从侧栏移到设置里，在新窗口打开。
- 「模板库」与「标签设计」合并为「标签」：打开系统落在画廊，点一张直接进整页编辑器。
  画廊里每张标签按真实物理比例呈现，绑定了数据源的代入第一行。编辑器左上有「返回标签」。
- 「模板」这个词从界面里退出，统一叫「标签」。接口字段不变。
- **旧地址继续可用**：`/design/{id}?preset=…` 与 `/templates` 会跳到新地址，查询串保留。
  资产台账那边的链接不用改。

### 编辑：离开即放弃，最后保存为准

- 编辑器不再保留未保存的修改：点返回或侧栏离开时，若有修改会先问「保留这些修改吗？」，
  留下可以先保存，离开就丢掉。刷新页面同样会问。
- 两个人同时改一张标签时，以**最后一次保存**为准：服务器拒绝过期版本的保存时，客户端会
  取当前版本再保存一次，不再要求先重新载入。

### 打印确认

- 打印确认框按真实比例画出标签在打印头上的位置，超出的部分标红。没探测过的打印机不画。
